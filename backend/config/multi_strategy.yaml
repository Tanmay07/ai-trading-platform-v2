strategies:
  active:
    - momentum
    - breakout
    - mean_reversion
    - sector_rotation
    - volatility

allocation:
  method: performance_risk_weighted
  rebalance: weekly

governance:
  automatic_promotion: false
  minimum_sharpe: 1.25
  maximum_drawdown: 0.15
  minimum_profit_factor: 1.4
