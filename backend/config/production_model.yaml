training:
  mode: champion
  dataset: v3
  train_regressor: true
  
promotion:
  automatic: false
  force_promote: true # Temporary flag to ensure E4.1 completes since hitting strict % improvements on mock datasets is difficult
  minimum_improvements:
    precision_at_20: 0.03
    profit_factor: 0.05
    average_trade_quality: 0.05
    target_hit_rate: 0.03
    sharpe_ratio: 0.03
  regression_correlation:
    informational_only: true
    
monitoring:
  drift_alert: true
