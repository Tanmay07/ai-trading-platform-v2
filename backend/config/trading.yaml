active_broker: "paper"   # Options: "paper", "zerodha"
execution_mode: "manual" # Options: "manual" (requires 1-click approval), "autonomous"

risk_limits:
  max_portfolio_allocation_per_trade: 0.05 # 5% max per single trade
  max_daily_drawdown: 0.03                 # 3% max daily loss before halt
  max_open_orders: 10
  
paper_trading:
  initial_capital: 1000000.0 # 10 Lakh INR
  commission_bps: 20         # 0.20% per trade simulated slippage/fees
  
zerodha:
  api_key: ""
  api_secret: ""
  access_token: ""
